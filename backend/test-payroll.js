const axios = require('axios');

async function test() {
  try {
    // We need a valid token to test. 
    // Wait, we don't have a token easily available.
  } catch (error) {
    console.error(error);
  }
}
test();
